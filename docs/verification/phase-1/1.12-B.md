# Phase 1.12-B — Bootstrap Credential Boundary

**Status:** Complete and verified.

## Verification

- `Hive.Tests`: **158 tests passed, 0 failed, 0 skipped** on **2026-09-22**.
- The full suite includes the focused bootstrap credential, configuration, persistence-options, and host-composition coverage introduced or updated for 1.12-B.
- Bootstrap credential storage remains outside the target Hive database.
- Windows DPAPI uses the current-user scope.
- Bootstrap references are distinct from Hive database-backed `SecretReference` values.
- `hive-settings.json` persists only the bootstrap credential identifier/reference and does not persist raw SQL password material.
- Legacy `credentialSecretId` configuration is rejected explicitly rather than silently reinterpreted.
- Missing/corrupt/undecryptable bootstrap material is represented through typed failures without returning the credential value.
- Password replacement creates a new bootstrap credential reference before publishing the new persistence configuration, then removes the old reference only after the configuration save succeeds.
- Resolved `SecretMaterial` is disposed after use by the composition/Management caller.
- No new Example was required for this infrastructure sub-stage. The real Settings/configured-host acceptance flow remains deferred to later Phase 1.12 sub-stages.

## Developer-provided test result

Captured on **2026-09-22**:

```text
========== Starting test run ==========
[xUnit.net 00:00:00.00] xUnit.net VSTest Adapter v3.1.4+50e68bbb8b (64-bit .NET 10.0.1)
[xUnit.net 00:00:00.28]   Starting:    Hive.Tests
[xUnit.net 00:00:05.31]   Finished:    Hive.Tests
========== Test run finished: 158 Tests (158 Passed, 0 Failed, 0 Skipped) run in 5.3 sec ==========
```
